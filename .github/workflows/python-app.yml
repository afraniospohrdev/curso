name: Python application

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Install Poetry
      run: pip install poetry

    # Instala apenas dependências, sem empacotar o projeto
    - name: Install dependencies
      run: poetry install --no-root

    - name: Run tests
      run: poetry run pytest

    # Etapa de build Docker
    - name: Build Docker image
      run: docker build -t fastapi-app:latest .

    - name: Run Docker container (smoke test)
      run: |
        docker run --rm -d -p 8000:8000 --name fastapi-test fastapi-app:latest
        sleep 5
        docker ps | grep fastapi-test
        docker stop fastapi-test

      
